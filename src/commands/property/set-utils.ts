// TODO: implement logic for adding the property to the file
export const addProperty = (
    key: string,
    value: string,
    secret: boolean,
    global: boolean
) => {
    // throw Error('OOPSIE!!!!');
    return true;
};
